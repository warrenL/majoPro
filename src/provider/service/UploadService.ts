import { Injectable } from '@angular/core';

/**
 * The class used to provide serive api for upload.
 *
 */
@Injectable()
export class UploadService {

  startUpload() {
    // Get unupload data
    // Upload to server
    console.log("startUpload");
  }
}
